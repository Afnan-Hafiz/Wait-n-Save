{
  "name": "wait-n-save",
  "version": "1.0.0",
  "description": "A browser extension + backend service that watches the products you care about and tells you exactly when it's worth buying.",
  "main": "index.js",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "build:backend": "npm run build --prefix backend",
    "typecheck:backend": "npm run typecheck --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "typecheck:frontend": "npm run typecheck --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "migrate": "npm run migrate --prefix backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Afnan-Hafiz/Wait-n-Save.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Afnan-Hafiz/Wait-n-Save/issues"
  },
  "homepage": "https://github.com/Afnan-Hafiz/Wait-n-Save#readme",
  "dependencies": {
    "@neon/config": "^1.7.2",
    "@neon/env": "^1.4.3"
  }
}
